import 'package:flutter/material.dart';

class MobileFeedsScreen extends StatelessWidget {
  const MobileFeedsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
