import 'package:flutter/material.dart';

class MobileMyFeedsScreen extends StatelessWidget {
  const MobileMyFeedsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
