import 'package:flutter/material.dart';

class DefaultBottom extends StatelessWidget {
  const DefaultBottom ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
